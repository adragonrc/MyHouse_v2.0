package com.alquilerapp.myapplication.mydialog;

public class PresenterDialogOptions {
   /* Context mContext;
    FragmentManager manager;
    public PresenterDialogOptions(Context mContext, FragmentManager manager){
        this.manager = manager;
        this.mContext = mContext;
    }
    @Override
    public void OnClickOption1() {
        Toast.makeText(mContext, "Op1", Toast.LENGTH_SHORT).show();
    }

    @Override
    public void OnClickOption2(String idAlquiler) {
        Intent i = new Intent(mContext, TableActivity.class);
        i.putExtra(TAlquiler.ID, idAlquiler);
        mContext.startActivity(i);
    }

    @Override
    public void OnClickOption3(String idAlquiler) {
        DialogImput imput = new DialogImput();
        final String id= idAlquiler;
        imput.showDiaglog(manager, "d", new PresenterDialogImput(mContext, "ALERTA") {
        @Override
        public void positiveButtonListener(@Nullable String s) {
            }
        });
    }*/
}
